#pragma once

#include <util/xcb.hpp>
#include <structures/connection.hpp>
#include <structures/request_buffer.hpp>

namespace fluke {

	// decltype(auto) window_teleport(const fluke::Connection& conn, xcb_window_t win, const fluke::Rect& rect) {
	// 	return fluke::SetWindowRect(conn, win, rect);
	// }

}
