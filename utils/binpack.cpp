#pragma once

#include <vector>
#include <string>

#include <tinge.hpp>
#include <fluke.hpp>

// Uses a bin packing algorithm to tile the windows on a focused monitor.

namespace fluke::utils {
	int binpack(fluke::Connection& conn, std::vector<std::string>& args) {

		return 0;
	}
}
