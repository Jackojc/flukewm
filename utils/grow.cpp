#pragma once

#include <vector>
#include <string>

#include <tinge.hpp>
#include <fluke.hpp>

// Grow a window by a given amount. The windows size will increase equally on all sides.

namespace fluke::utils {
	int grow(fluke::Connection& conn, std::vector<std::string>& args) {

		return 0;
	}
}
