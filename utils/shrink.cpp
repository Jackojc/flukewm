#pragma once

#include <vector>
#include <string>

#include <tinge.hpp>
#include <fluke.hpp>

// Shrink a window by a given amount. The windows size will decrease equally on all sides.

namespace fluke::utils {
	int shrink(fluke::Connection& conn, std::vector<std::string>& args) {

		return 0;
	}
}
